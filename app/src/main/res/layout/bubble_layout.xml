<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bubble_container"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- CARA 1: Fake Lag (El icono) -->
    <FrameLayout
        android:id="@+id/cara_fake_lag"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/bg_bubble">
        
        <ImageView
            android:id="@+id/bubble_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_play_white"
            android:tint="#FFFFFF" />
    </FrameLayout>

    <!-- CARA 2: NoRecoil Menu (Oculto) -->
    <LinearLayout
        android:id="@+id/recoil_menu"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:minHeight="220dp"
        android:visibility="gone"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="#000000">
        
        <LinearLayout
            android:id="@+id/menu_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">
            
            <ImageButton
                android:id="@+id/btn_back_to_lag"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_arrow_back"
                android:tint="#FFFFFF" />
        </LinearLayout>
        
        <Switch 
            android:id="@+id/recoil_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="NoRecoil External" 
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:thumb="@drawable/custom_seekbar_thumb"
            android:layout_marginTop="2dp" />

        <TextView 
            android:id="@+id/recoil_percentage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Efectividad: 50%" 
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:layout_marginTop="2dp" />

        <SeekBar 
            android:id="@+id/recoil_seekbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="50"
            android:progressDrawable="@drawable/custom_seekbar_track"
            android:thumb="@drawable/custom_seekbar_thumb"
            android:splitTrack="false"
            android:layout_marginTop="4dp" />

            <!-- Añade esto justo debajo del SeekBar de NoRecoil -->

        <Switch 
            android:id="@+id/fov_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FOV External" 
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:thumb="@drawable/custom_seekbar_thumb"
            android:layout_marginTop="8dp" />

        <TextView 
            android:id="@+id/fov_size_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Radio FOV: 100px" 
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:layout_marginTop="2dp" />

        <SeekBar 
            android:id="@+id/fov_seekbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="600"
            android:progress="0"
            android:progressDrawable="@drawable/custom_seekbar_track"
            android:thumb="@drawable/custom_seekbar_thumb"
            android:splitTrack="false"
            android:layout_marginTop="4dp" />
    </LinearLayout>
</FrameLayout>